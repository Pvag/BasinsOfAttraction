# plot a grid of points with rgb value using gnuplot
# ==================================================

set term png transparent size 4000,4000
set output "output.png"
set lmargin -2; set bmargin - 2; set tmargin 2; set rmargin 2
# set xlabel "Re(z)"; set ylabel "Im(z)"
unset border
unset xtics
unset ytics
rgb(r,g,b) = 50*int(r) + 5*int(g) + 100*int(b)
plot "basins.out" using 1:2:(rgb($4,$4,$4)) with dots lc rgb variable
